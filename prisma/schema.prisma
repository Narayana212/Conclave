// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}
datasource db {
  provider = "mysql"
  url ="mysql://gyl7nqzwuyupr8v5p9pd:pscale_pw_oK0B8oZJS9ZHGwXB0TIGPx3FSs5ecO71ynV0c7pxBQ2@aws.connect.psdb.cloud/users?sslaccept=strict"
  relationMode = "prisma"
}

model User {
  userId   String @id @default(uuid()) @unique
  fullName String
  email    String   @unique
  password String
  booking  Booking?
}

model Booking {
  booking_id String   @id @default(uuid()) @unique
  bookToken  String   @unique
  createdAt  DateTime
  user       User?    @relation(fields: [userId], references: [userId])
  userId     String @unique
}




